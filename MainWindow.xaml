<Window x:Class="PomodoroTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ctrl="clr-namespace:PomodoroTimer.Controls"
        Title="Pomodoro Timer"
        Width="580"
        Height="580"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextBrush}"
        WindowStartupLocation="CenterScreen"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Grid Background="{StaticResource BackgroundBrush}" Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ВРЕМЯ + СТАТУС -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock x:Name="TimeText"
                       Text="00:00"
                       FontSize="44"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0"/>
            <TextBlock x:Name="StatusText"
                       Text="Готов"
                       HorizontalAlignment="Center"
                       Margin="0,4,0,0"
                       Foreground="{StaticResource SubtleTextBrush}"/>
        </StackPanel>

        <!-- КНОПКИ УПРАВЛЕНИЯ -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,8">
            <Button x:Name="ToggleStartPauseButton"
                    Content="Start"
                    Width="110"
                    Margin="0,0,8,0"
                    Click="ToggleStartPauseButton_Click">
                <Button.ToolTip>
                    <ToolTip Content="Старт/Пауза (Ctrl+Alt+S)"/>
                </Button.ToolTip>
            </Button>
            <Button Content="Stop"
                    Width="90"
                    Margin="0,0,8,0"
                    Click="Stop_Click">
                <Button.ToolTip>
                    <ToolTip Content="Стоп (Ctrl+Alt+A)"/>
                </Button.ToolTip>
            </Button>
            <Button x:Name="SwitchModeButton"
                    Content="Сменить режим"
                    Width="140"
                    Click="SwitchModeButton_Click">
                <Button.ToolTip>
                    <ToolTip Content="Сменить Работа/Отдых (Ctrl+Alt+Q)"/>
                </Button.ToolTip>
            </Button>
        </StackPanel>

        <!-- АВТОПРОДОЛЖЕНИЕ + ИСТОРИЯ -->
        <StackPanel Grid.Row="2" Margin="4,0,0,10">
            <CheckBox x:Name="AutoContinueCheck"
                      Content="Auto-continue next period"/>

            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,8,0,0">
                <Button x:Name="PreviousDayButton"
                        Content="◀"
                        Width="40"
                        Margin="0,0,8,0"
                        Click="PreviousDayButton_Click">
                    <Button.ToolTip>
                        <ToolTip Content="Предыдущий день (←)"/>
                    </Button.ToolTip>
                </Button>
                <TextBlock x:Name="SelectedDateText"
                           VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Text="Сегодня"
                           Margin="0,0,8,0"/>
                <Button x:Name="NextDayButton"
                        Content="▶"
                        Width="40"
                        Click="NextDayButton_Click">
                    <Button.ToolTip>
                        <ToolTip Content="Следующий день (→)"/>
                    </Button.ToolTip>
                </Button>
            </StackPanel>
        </StackPanel>

        <!-- ПРЕСЕТЫ + ГИСТОГРАММА -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Presets -->
            <GroupBox Header="Presets" Grid.Row="0" Margin="0,0,0,10">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <ListBox x:Name="PresetList"
                             Grid.Row="0"
                             Margin="0,0,0,8"
                             DisplayMemberPath="Name"
                             SelectionChanged="PresetList_SelectionChanged"/>

                    <StackPanel Grid.Row="1"
                                Orientation="Horizontal"
                                HorizontalAlignment="Center">
                        <Button Content="Add" Width="70" Click="AddPreset_Click"/>
                        <Button Content="Edit" Width="70" Click="EditPreset_Click"/>
                        <Button Content="Delete" Width="70" Click="DeletePreset_Click"/>
                    </StackPanel>
                </Grid>
            </GroupBox>

            <!-- Chart -->
            <GroupBox Grid.Row="1">
                <GroupBox.Header>
                    <DockPanel LastChildFill="False">
                        <TextBlock Text="Помидоро за выбранный день"
                                   VerticalAlignment="Center"/>
                        <Button Content="Статистика по месяцам"
                                DockPanel.Dock="Right"
                                Click="OpenMonthlyStats_Click"
                                Margin="8,0,0,0">
                            <Button.ToolTip>
                                <ToolTip Content="Открыть статистику по неделям"/>
                            </Button.ToolTip>
                        </Button>
                    </DockPanel>
                </GroupBox.Header>
                <Grid>
                    <ctrl:DailyChartControl x:Name="DailyChart"
                                            MinHeight="140"
                                            Margin="0"/>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</Window>

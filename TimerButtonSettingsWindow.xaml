<Window x:Class="PomodoroTimer.TimerButtonSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройка кнопок"
        Width="520"
        Height="520"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource TextBrush}">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0"
                      VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="ButtonsRepeater"
                          Margin="0,0,0,12">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10"
                                Margin="0,0,0,10"
                                Background="{DynamicResource CardBrush}"
                                CornerRadius="6"
                                BorderBrush="{DynamicResource BorderBrushColor}"
                                BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0"
                                            Margin="0,0,8,0">
                                    <TextBlock Text="Название"
                                               Foreground="{DynamicResource SubtleTextBrush}"/>
                                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

                                    <CheckBox Content="Это отдых" IsChecked="{Binding IsRest}"
                                              Margin="0,6,0,0"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Цвет кнопки (Hex)"
                                               Foreground="{DynamicResource SubtleTextBrush}"/>
                                    <TextBox Text="{Binding BackgroundColorHex, UpdateSourceTrigger=PropertyChanged}"/>

                                    <TextBlock Text="Цвет текста (Hex)"
                                               Foreground="{DynamicResource SubtleTextBrush}"
                                               Margin="0,6,0,0"/>
                                    <TextBox Text="{Binding TextColorHex, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Width="100"
                    Margin="0,0,8,0"
                    Click="Ok_Click"/>
            <Button Content="Отмена" Width="100" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
